{
  "devDependencies": {
    "tailwindcss": "^3.4.11"
  },
  "builds": [
    { "src": "package.json", "use": "@vercel/static-build" }
  ],
  "scripts": {
    "build": "tailwindcss build src/input.css -o src/output.css"
  }
}
